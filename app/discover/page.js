import DiscoverClient from './DiscoverClient';

export const metadata = {
  title: 'Campus popular — shared study decks',
  description: 'Browse the most-studied shared flashcard decks across Reclipse, by course. Free, anonymous, no login needed.',
};

export default function DiscoverPage() {
  return <DiscoverClient />;
}
