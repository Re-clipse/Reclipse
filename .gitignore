# === SECRETS — never commit these ===
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.production
!.env.example

# dependencies
node_modules/
/.pnp
.pnp.js

# next.js build output
/.next/
/out/
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/
.idea/

# design-tool scratch (Superdesign CLI project state, local skill config)
.claude/
.superdesign/

# /brag promo-video output (large local media, not app source)
brag-output/
brag-output-*/

# test artifacts (traces, screenshots, videos, html reports) — regenerated
# by `npm run test:e2e`, uploaded as CI artifacts on failure, never committed
/test-results/
/playwright-report/
/blob-report/
/coverage/
